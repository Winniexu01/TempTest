trigger: none

pr:
  branches:
    include:
    - main

pool:
  vmImage: 'windows-latest'

stages:
- stage: Test
  displayName: Test Stage
  jobs:
  - job: Test
    displayName: 'Test'
    steps:
    - pwsh: |
        Write-Host "This is a test pipeline."
      displayName: 'Run Test Script'