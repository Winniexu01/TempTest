parameters:
- name: SolutionName
  type: string
  default: ' '

steps:
- task: NuGetToolInstaller@1

- pwsh: |
    Write-Host "Testing solution: ${{ parameters.SolutionName }}"  
  displayName: 'Build ${{ parameters.SolutionName }}'